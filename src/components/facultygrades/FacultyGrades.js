import React from 'react';
import './FacultyGrades.scss';
// import { useParams } from 'react-router-dom';

const FacultyGrades = function() {
	// const { course, section } = useParams();

	return (
		<div className="facultygrades">
			<h2>
				faculty grades should allow faculty to add scores to particular assignments, though I'm pretty sure you
				would need a list of students first.
			</h2>
		</div>
	);
};

export default FacultyGrades;
